import { PriorityFormatPipe } from './priority-format.pipe';

describe('PriorityFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new PriorityFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
